version: "3.3"
services:
  backend:
    environment:
      STAGE: production
      SERVER_PORT: 3000
    build:
      context: .
      dockerfile: ./Dockerfile.local
      target: prod
    ports:
      - 3000:3000
