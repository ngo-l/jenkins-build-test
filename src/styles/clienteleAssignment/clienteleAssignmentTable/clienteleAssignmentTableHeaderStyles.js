import { Colors } from '../../styleGuides'

export const ClienteleAssignmentTableHeaderStyles = {
  sortLabel: {
    '& .MuiTableSortLabel-icon': {
      color: `${Colors.system.black.color} ! important`
    }
  }
}
